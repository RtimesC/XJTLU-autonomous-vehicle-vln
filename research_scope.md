# 端到端视觉语言导航研究范围

状态：v0.1 暂定基线

更新日期：2026-09-10

## 1. 研究目标

本项目研究一个运行在真实轮式机器人上的语言条件导航策略。策略接收前视 RGB 图像、自然语言路线指令和有限历史信息，直接预测底盘线速度、角速度与停止决策，在不依赖预建地图和传统路径规划器完成路线决策的条件下执行导航任务。

核心策略写作：

```text
(RGB observation, language instruction, action history)
    -> VLN policy
    -> (linear velocity, angular velocity, stop probability)
```

本研究中的“端到端”指从视觉与语言观测到机器人运动动作的直接策略映射。底盘轮速闭环、电机电流控制、通信保护和物理急停仍由传统控制与安全系统负责，不属于学习策略范围。

## 2. 主实验边界

### 2.1 策略允许使用的输入

主实验允许：

- 单个前视 RGB 相机的图像；
- 不包含坐标或机器可读路径点的自然语言路线指令；
- 最近若干步的历史动作；
- 策略自身维护的视觉或语言历史状态；
- episode 标识、开始、取消等运行控制信息。

相机型号和物理 ROS topic 不属于研究定义，必须通过部署配置和 ROS remapping 接入逻辑接口 `/vln/input/image`。

### 2.2 主策略禁止使用的输入

以下信息不得进入主实验策略：

- SLAM 或 FAST-LIO2 位姿；
- `map -> odom`、全局位姿或 GPS/RTK 坐标；
- Nav2 global plan、local plan、costmap 或 recovery 状态；
- 人工标注的在线目标方向、航点或轨迹；
- 雷达计算得到的可通行方向或自动绕障决策。

这些信息可以用于基线、安全监测、实验裁判和离线评测，但必须与策略输入在代码、topic 和日志中保持可审计的隔离。

### 2.3 暂定消融变量

以下输入只能以明确命名的消融实验出现，不能与主实验结果混报：

- RGB-D 或独立深度图；
- IMU、轮速计或机体速度；
- 不同历史窗口长度；
- 不同视觉编码器、语言编码器或记忆结构；
- 单步动作与短动作序列输出。

## 3. 动作空间

主策略在每个决策时刻输出：

```text
a_t = (v_t, omega_t, p_stop_t)
```

- `v_t`：ROS `base_link` 前向线速度，单位 m/s；
- `omega_t`：绕 z 轴角速度，单位 rad/s，遵循 ROS REP-103；
- `p_stop_t`：任务完成并停车的概率，范围 `[0, 1]`。

策略不得直接预测电机电流、独立车轮转速、Nav2 goal 或路径点。连续动作的有效范围、停止阈值和停止确认窗口由实验配置记录，不在本文件中写死。

## 4. 指令与任务定义

主实验使用自由形式的自然语言路线指令，可以包含多个地标、转向和相对位置描述，例如“经过大厅后在第二个路口左转，在电梯前停下”。

指令不得包含：

- GPS 经纬度；
- `map` 坐标；
- 预计算航点数组；
- 可由程序直接执行的转向序列。

每次实验称为一个 episode。episode 必须具有唯一 ID、固定指令、起点定义、成功区域、最长时间、人工接管记录和明确的终止原因。

## 5. 停止与成功定义

停止决定由模型的 `p_stop` 产生，外部定位不得替模型触发正常任务完成。确定性的阈值和连续帧确认可以由动作适配器实现，但其配置必须保存，并同时保留原始 `p_stop`。

一次 episode 暂定满足以下条件时判为成功：

- 策略主动发出有效停止决定；
- 停止位置位于该场景预先定义的成功区域内；
- 未发生碰撞；
- 未发生人工接管；
- 未因 watchdog、模型错误或系统错误终止。

成功区域的几何定义和允许误差由具体实验场景协议确定，不在本文件中统一假设。

## 6. 安全层边界

模型外安全层可以：

- 限制最大线速度和角速度；
- 限制线加速度和角加速度；
- 对无效值、过期动作和推理超时输出零速度；
- 根据近距离障碍触发紧急停车；
- 响应人工接管与物理急停；
- 在失去控制权时阻止 VLN 命令到达底盘。

安全层不得：

- 规划绕障路线；
- 根据地图或点云选择新的行驶方向；
- 调用 Nav2 替策略完成局部导航；
- 修改语言目标；
- 将失败轨迹自动修正后作为原始策略成功上报。

所有安全干预必须带时间戳和原因记录。实验报告必须区分原始策略动作、安全处理后的动作和最终底盘动作。

## 7. 基线与实验组

至少保留以下可比较系统：

1. `VLN-E2E-RGB`：本项目主实验；
2. `SLAM/Nav2 baseline`：原车传统导航链路；
3. `VLN-E2E + depth`：深度输入消融；
4. `VLN-E2E + proprioception`：IMU、轮速或机体速度消融；
5. 其他模型、历史长度和动作表示消融。

传统基线可以使用地图、定位和规划，但不得与主 VLN 策略同时争用底盘命令。

## 8. 评测指标

必须记录：

- Success Rate；
- SPL 或适合真实路线的路径效率指标；
- 最终停止位置到目标区域的距离；
- 碰撞次数与严重度；
- 人工接管次数；
- 安全层拦截次数和原因；
- 线速度与角速度抖动；
- 推理延迟分布和 P95；
- 动作超时率、丢帧率和系统错误率；
- seen 与 unseen 场景的独立结果。

评测必须同时保存原始策略输出、安全过滤输出和最终 `/cmd_vel`，不得只报告经过安全层修正后的结果。

## 9. 开发与验证阶段

按以下顺序推进：

1. 离线数据与 rosbag 回放；
2. shadow mode，只推理和记录，不申请车辆控制权；
3. 仿真或硬件在环测试；
4. 抬轮测试和断链归零测试；
5. 低速、封闭、有人值守实车测试；
6. 分阶段扩展场景与速度范围。

任何阶段通过都不能自动证明下一阶段安全。

## 10. 暂不属于本项目的目标

- 研究新的 SLAM、回环检测或定位算法；
- 将 VLN 实现为 Nav2 goal 或 waypoint 生成器；
- 学习电机电流或车轮级实时控制；
- 第一阶段在开放道路、高速或无人值守条件下运行；
- 用安全层规划能力替代 VLN 策略能力；
- 在没有原始日志的情况下仅凭最终成功率得出结论。

## 11. 尚需实测确认的工程参数

- 实际相机型号、安装位置、内参与图像 topic；
- Jetson 上可持续运行的模型大小和精度；
- shadow mode 与实车模式的推理频率；
- 实车模式的最大 P95 推理延迟；
- Jetson 策略 watchdog 与 STM32 命令 watchdog 的最终阈值；
- 各场景成功区域、最大时长和速度上限。

这些参数必须通过硬件检查、资源测试和低速验证确定，不能从文档默认值直接推断。
